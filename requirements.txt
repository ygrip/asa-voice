# Full local (STT+TTS) dependency set, for `pip install -r requirements.txt` without Docker.
# Docker builds use the split requirements-{base,hosted,local-stt,local-tts}.txt files instead
# (setara-nx07.5, plan §12) so the hosted image can exclude torch/pocket-tts/faster-whisper.
-r requirements-local-stt.txt
-r requirements-local-tts.txt
